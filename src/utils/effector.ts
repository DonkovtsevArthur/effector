import * as effectors from "effector";
import * as reactEffector from "effector-react";

export const createEffect = effectors.createEffect;
export const createStore = effectors.createStore;
export const useStore = reactEffector.useStore;
