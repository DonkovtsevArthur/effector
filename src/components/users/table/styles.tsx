import styled from "~/styles/styled-components";
import { rem } from "~/styles/utils";

const Ul = styled.ul`
  display: block;
  width: 100%;
  background: #cccc;
  margin: 0;
  margin-left: ${rem("10px")};
`;

export default Ul;
