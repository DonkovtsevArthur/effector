import styled from "~/styles/styled-components";
import { rem } from "~/styles/utils";
export const UsersFormStyled = styled.div`
  display: flex;
  margin-top: ${rem("20px")};
  padding-left: ${rem("40px")};
  padding-right: ${rem("40px")};
`;
